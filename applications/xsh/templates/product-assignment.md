Complete the sealed Product replenishment assignment ${ASSIGNMENT_ID}.

Trusted target and evidence map:

${TARGET}

Work only from the current assigned XSH checkout. Discover a user-observable behavior defect rather
than a refactor, cleanup preference, seeded task, or implementation plan. Before each submission,
search the live ticket buffer and carry the exact duplicate-search input in the proposal. Submit no
more than three proposals. Use the admitted `reproducer` command profile and a sealed XSH program as
its stdin; the kernel must observe the same failure twice on the clean base. A proposal must remain
unsponsored until the external Architect makes a separate typed decision.
