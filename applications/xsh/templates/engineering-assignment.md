Complete the sealed Engineering assignment ${ASSIGNMENT_ID}.

Trusted target and evidence map:

${TARGET}

The ticket revision and base snapshot are immutable. Do not broaden, rewrite, or substitute the
problem. Keep the regression checkpoint free of an implementation fix. Make the smallest root fix
and any required canonical documentation update. Your final `candidate_submit` supplies only the
sealed Engineering report, sealed risks, normalized commit message, and regression test identity;
the kernel derives every repository identity from the assigned worktree. Do not commit, change HEAD,
run a formatter/autofixer, invoke pre-commit, or contact a Git remote.
